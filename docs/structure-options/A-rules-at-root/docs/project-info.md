# 프로젝트 정보 (Project Info)

> `claude init`이 생성하는 프로젝트 고유 산출물 자리. 루트 `CLAUDE.md`(공통규칙)에서 `@docs/project-info.md`로 import된다.
> 아래는 채워 넣을 항목의 placeholder다.

## 개요 (Overview)

이 프로젝트가 무엇이고 왜 존재하는지 한두 문단.

## 빌드 · 실행 · 테스트 (Build / Run / Test)

```bash
# 설치
# 빌드
# 실행
# 테스트
```

## 구조 (Architecture)

주요 디렉토리·모듈과 책임. 데이터 흐름.

## 컨벤션 (Conventions)

네이밍·포맷·커밋 규칙 등 이 레포 고유의 약속.
